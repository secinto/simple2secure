version: "3" 
services:  
    flask:
        build: .
        command: "python3 app.py"
        ports:
            - 5001:5000
        links:
            - redis
        volumes:
            - .:/usr/src/app

    redis:
        image: redis
        ports:
            - 6379:6379